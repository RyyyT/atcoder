#include <bits/stdc++.h>
using namespace std;
#define rep(i, s, n) for (int i = (s); i < (int)(n); i++)

int N, B, K;
int mod;
vector<int> c(K);

int main() {
    mod = pow(10, 9) + 7;
    cin >> N >> B >> K;
    rep(i, 0, K) cin >> c.at(i);

    
    return 0;
}