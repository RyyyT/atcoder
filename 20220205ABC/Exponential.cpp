#include <bits/stdc++.h>
using namespace std;
#define rep(i, s, n) for (int i = (s); i < (int)(n); i++)

int main() {
    int64_t N;
    cin >> N;
    if(N < 2 || 4 < N) cout << "Yes" << endl;
    else cout << "No" << endl;
}
